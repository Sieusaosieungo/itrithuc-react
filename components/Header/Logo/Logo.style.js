import styled from 'styled-components';

const LogoStyle = styled.div`
  padding: 8px;
  height: 80%;
  border-radius: 5px;

  img {
    height: 100%;
    cursor: pointer;
  }
`;

export default LogoStyle;
